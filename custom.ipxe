#!ipxe
###
### netboot.xyz-custom menu
###

:custom
clear os
menu OS
item --gap Preseed OS
item ubuntu
choose os || goto custom_exit
echo ${cls}

chain https://raw.githubusercontent.com/jescholl/netboot.xyz-custom/master/${os}.ipxe || goto custom
